import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'playMemoryApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
